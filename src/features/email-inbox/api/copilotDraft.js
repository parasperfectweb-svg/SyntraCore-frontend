// features/email-inbox/api/copilotDraft.js
// The Copilot-generated reply draft — same illustrative "captured from
// this email" demo data as the rest of the Copilot panel. Shared so the
// CopilotPanel's "Copy"/"Use in Reply" buttons and the reply box's own
// "Reply with Copilot" shortcut stay in sync. Wire this to a real
// extraction API later without changing either call site.
export const COPILOT_DRAFT_TEXT = "Dear Priya,\n\nWe will need the following additional details to finalize the quotation:\n1. HS Code / Commodity Code\n2. Complete shipper's address\n3. Insurance requirements\n\nOur team will send the formal quotation within 24 hours upon receipt.";
