// app/providers/QueryProvider.jsx
// Placeholder for React Query — no server data-fetching is wired up yet
// (see api/client.js), but every feature's hooks/use*.js is already
// structured so swapping in real queries later is a local change, not an
// architectural one. Install @tanstack/react-query and flesh this out
// when the real API is ready; until then it's a harmless passthrough.
export function QueryProvider({ children }) {
  return children;
}
